# Service config
service:
  name: lirest-iam
  host: 127.0.0.1
  port: 3030
  baseUrl: /api/v1/iam

# Database configuration
mongodb:
  uri: mongodb://localhost:27017/lirest

# Swagger username and password
# api-docs:
#   username: ''
#   password: ''

# Log configuration
log:
  level: info

# JWT config
# jwt:
#   accessTokenKey: ''
#   refreshTokenKey: ''
#   accessTokenExpiresIn: 1h
#   refreshTokenExpiresIn: 1d

# Kafka config
kafka:
  clientId: lirest-iam
  groupId: lirest-iam-group
  serverUrl: kafka://localhost:9092
  sasl:
    username: ''
    password: ''
  enabled: false